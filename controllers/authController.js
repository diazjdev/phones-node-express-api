const jwt = require('jwt-simple');

const signUp = (req, res) => {
	const { body: { email, password }
	} = req;



};

const signIn = () => { };
const signOut = () => { };

module.exports = {
	signUp,
	signIn,
	signOut
}
